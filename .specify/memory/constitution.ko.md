# 📜 프로젝트 헌법 (Constitution)

이 문서는 프로젝트의 **절대적인 원칙**과 **의사결정 기준**을 정의합니다.

## 1. 💎 핵심 가치 (Core Values)
1.  **단순함이 최고다 (Simplicity)**: 복잡한 코드보다 읽기 쉬운 코드가 낫습니다.
2.  **SSOT (Single Source of Truth)**: 데이터 구조의 정답은 `schema.prisma`입니다.
3.  **타입은 문서다**: `any` 사용은 금지됩니다.

## 2. 🏛 아키텍처 및 폴더 구조 (Architecture)
1.  **소스 코드 (Src Standard)**: `src`는 직관적인 표준 구조를 따릅니다.
2.  **테스트 코드 (Test History)**: `test`는 TDD 단계(`Red/Green/Refactor`)를 보존합니다.

## 3. 🛡 TDD 및 개발 프로세스 (Process)

**테스트 작성 시 "구조화된 로그"를 필수적으로 포함해야 합니다.**
테스트 실행 결과만 보고도 명세를 알 수 있어야 합니다.

1.  **Test Wrapper 사용**: 모든 테스트는 `runTest` 헬퍼를 통해 실행합니다.
2.  **필수 명시 항목**:
    *   **Feature**: 기능 단위
    *   **Title**: 테스트 이름
    *   **Purpose**: 테스트 목적
    *   **Params**: 입력 파라미터
    *   **Expected**: 예상 결과

### A. 새로운 기능 추가 시 (New Feature)
1.  **문서화 단계 (Documentation Phase)**
    *   **User용 (Korean)**: `.ko.md` 확장자 사용. (예: `spec.ko.md`)
    *   **AI용 (English)**: `.md` 확장자 사용. (예: `spec.md`)
    *   **필수 문서**: `Spec`, `Plan`, `Data Model`.
2.  **테스트 폴더 준비**: `test/{feature}/Red` 생성 및 실패 테스트 작성.
3.  **소스 폴더 작성**: `src/{feature}/` 구현.

### B. 기존 기능 수정 시 (Existing Feature)
1.  **문서 업데이트**: 국문/영문 문서 모두 수정.
2.  **TDD 사이클**: Red -> Green -> Refactor.
3.  **소스 수정**: 마지막에 반영.

## 4. 📚 API 및 문서화 (Swagger)
1.  **구조 (Unified Docs)**
    *   `docs/{feature}/` 폴더에 JSON 기반 Swagger 문서 관리.
    *   UI 상에서는 통합되어 보여야 함.
2.  **URL 정책**
    *   **API**: `/api/v1/{resource}`
    *   **Docs**: `/api` 접속 시 Swagger UI로 리다이렉트.
3.  **작성 시점 (Post-Implementation)**
    *   Swagger 문서는 **소스(Green/Refactor) 완료 후**에 작성합니다.
    *   **필수 포함**: 테스트 과정에서 발견된 **모든 예외 케이스(Exception Cases)**를 문서에 명시해야 합니다.

## 5. 🤖 AI 협업 가이드
1.  **문서 확인**: 작업을 시작하기 전에 3대 문서가 모두 존재하는지 확인하십시오. 하나라도 빠져있다면 먼저 작성해야 합니다.
2.  **순서 엄수**: 문서 -> 테스트 -> 소스 -> **Swagger** 순서를 지키십시오.
